
# 📖 Codyssey_7_ProJect 초심자용 용어 치트 시트

> 이 프로젝트 한 줄 요약:  
>"야후 파이낸스에서 삼성전자 2년치 주가를 가져와서, 통계 기법으로 정리하고, 7개의 그래프와 웹 대시보드로 '언제 사고 팔아야 하는가'를 정량적으로 분석한 프로젝트"

---

## 0️⃣ 프로젝트 구조 용어 (코드를 모르는 분을 위해)

| 용어 | 쉬운 설명 |
|---|---|
| **데이터** | 이 프로젝트의 원재료. 날짜별 주가(시가/고가/저가/종가)와 거래량이 담긴 표. 총 489개 거래일치 (`samsung_2023_2024.csv`) |
| **스크립트 파일** | 파이썬 명령어들을 순서대로 적어놓은 실행 파일(`.py`). 요리로 치면 '레시피 종이'. 이 프로젝트엔 `collect_data.py`(수집), `analysis.py`(분석), `dashboard.py`(대시보드) 3개가 있음 |
| **파이프라인** | 데이터가 흘러가는 공정 라인. **수집(①) → 저장(CSV) → 정제 → 분석(②) → 시각화(③)** 순서로 이어지는 전체 흐름. 공장 컨베이어벨트처럼 앞 단계의 결과물이 다음 단계의 재료가 됨 |
| **보일러플레이트(Boilerplate)** | 매번 똑같이 필요한 '기본 뼈대 코드'. 차트 설정, 서버 실행 코드처럼 반복되는 부분. 이 프로젝트에서는 AI가 이 뼈대를 만들어주고, 사람은 분석 로직과 파라미터 결정에 집중했다고 리포트에 명시됨 |
| **Dash 기반 인터랙티브 분석 환경** | `dashboard.py`가 만드는 **웹 대시보드**. 브라우저에서 `http://127.0.0.1:8050`으로 접속하면, 마우스로 날짜 범위를 고르고 슬라이더로 이동평균 기간(5~120일)을 바꾸면서 캔들차트·거래량 차트가 실시간으로 바뀌는 화면 |

---

## 1️⃣ 데이터의 기본 재료 용어

| 용어 | 쉬운 설명 |
|---|---|
| **변수** | 표의 '열(column)' 하나하나. 이 데이터에선 `close`(종가), `open`(시가), `high`(고가), `low`(저가), `volume`(거래량)이 변수 |
| **종속변수** | "내가 알고 싶은 대상". 다른 변수들의 영향을 받는 결과값. 이 프로젝트에서는 **종가(주가)** 가 종속변수 — 거래량, 시간 등에 따라 변하는 값이니까요 |
| **종가** | 하루 장이 끝날 때의 최종 가격. 주가 분석에서 가장 대표적으로 쓰는 값. 모든 분석(이동평균, 수익률, 변동성)의 기준 |
| **거래량** | 그날 주식이 몇 주나 사고팔렸는가. "시장의 관심도". 가격이 움직일 때 거래량이 같이 터지면 '진짜 움직임'으로 해석 (예: 2023-01-04 거래량 2,019만 주) |
| **수정종가** | 액면분할, 배당 같은 이벤트를 반영해 과거 주가를 보정한 값. 그래야 분할 전후 가격이 끊기지 않고 연속적으로 비교됨. yfinance가 자동으로 보정해 줌 |
| **데이터** (CSV) | 위 변수들이 날짜순으로 489행 쌓인 표 파일 |

---

## 2️⃣ 데이터 정제 (분석 전 청소 단계)

| 용어 | 쉬운 설명 |
|---|---|
| **데이터 정제** | 분석하기 전에 데이터의 '빈틈'과 '튀는 값'을 처리하는 청소 작업. 더러운 데이터로 분석하면 결론도 더러워지므로 필수 단계 |
| **결측치** | 데이터가 비어있는 칸(NaN). 이 프로젝트의 원본엔 결측치가 0개였지만, 시계열 분석(ACF, 분해)을 위해 주말·공휴일까지 날짜를 쫙 펼치면(`asfreq('B')`) 비어있는 날이 생김 → **`ffill`(Forward Fill, 전날 값으로 채우기)** 로 처리 |
| **이상치(Outlier)** | 나머지 값들과 동떨어진 '튀는 값'. 오타일 수도, 진짜 급등락일 수도 있음 |
| **Z-score 3** | 이상치 판별 기준 ①. "평균에서 표준편차의 3배 이상 떨어지면 이상치". 정규분포에서 99.7%가 ±3 안에 들어오므로, 밖에 있으면 '보통 일이 아니다'라고 보는 것 |
| **IQR 1.5** | 이상치 판별 기준 ②. 데이터를 4등분했을 때 가운데 50%(IQR) 범위의 1.5배를 벗어나면 이상치. 박스플롯의 '수염' 바깥 값 |
| → 이 프로젝트의 결론 | 두 기준으로 검사했지만 2023~2024 삼성전자 주가의 급등락은 ±10% 이내라 **제거할 이상치 없음** 판정 |

---

## 3️⃣ 기초 통계 용어

| 용어 | 쉬운 설명 |
|---|---|
| **통계량** | 데이터를 요약한 숫자들의 총칭 (평균, 표준편차 등) |
| **평균값** | 모든 값을 더해 개수로 나눈 것. "대표값"이지만 극단값에 약함 |
| **중앙값** | 크기순으로 줄 세웠을 때 한가운데 값. 극단값에 강함 (평균보다 왜곡이 적음) |
| **최빈값** | 가장 자주 나온 값 |
| **표준편차** | 값들이 평균에서 얼마나 흩어져 있는가. 주식에서는 **"불안정성 = 변동성"** 의 척도. 크면 위험한 종목 |
| **상관계수** | 두 변수가 같이 움직이는 정도 (−1 ~ +1). +1에 가까우면 같은 방향, 0이면 무관계 |

---

## 4️⃣ 분석의 뼈대: 가설과 지표

| 용어 | 쉬운 설명 |
|---|---|
| **가설(Hypothesis)** | "검증하고 싶은 주장". 이 프로젝트는 4개의 질문(Q1~Q4)을 세우고 데이터로 검증함. 예: Q1 "2023년 대비 2024년은 하락 추세인가?" |
| **정량적 지표** | 감(感)이 아니라 숫자로 된 판단 기준. "떨어진 것 같다"❌ → "120일선 대비 −5% 이탈"⭕ |
| **정량적 Action (Insights)** | 분석 결과를 실제 행동 규칙으로 번역한 것. 이 프로젝트 리포트 6장의 핵심: "IF(정량 조건) → THEN(기계적 행동)" 형태의 매매 규칙 3가지 |
| **파생변수** | 원본 변수를 가공해 새로 만든 변수. 예: 종가 → 20일 이동평균(SMA_20), 월별 수익률, 롤링 표준편차. 코드에서 `df['SMA_20'] = ...` 처럼 추가되는 열들 |
| **통계 모델** | 데이터의 패턴을 수식으로 표현한 것. 이 프로젝트에선 시계열 분해(곱셈 모델), Naive 예측이 해당 |

---

## 5️⃣ 이동평균선 계열 (Q1 검증 도구)

| 용어 | 쉬운 설명 |
|---|---|
| **이동평균선(이평선, SMA)** | 최근 N일 종가의 평균을 매일 이어 그은 선. 하루하루 출렁임을 '다듬어서' 방향만 보여주는 선. 코드: `df['close'].rolling(window=20).mean()` |
| **20일선 / 60일선 / 120일선** | 약 1개월 / 1분기 / 반년의 평균 매수가격. 단기~장기 추세를 각각 보여줌. **장기 이평선**(120일)일수록 굵은 흐름, **단기**(20일)일수록 민감 |
| **정배열** | 단기선 > 장기선 순으로 위에 있는 상태 (20일 > 60일 > 120일). **상승 추세** 신호 |
| **역배열** | 반대로 장기선이 위에 있는 상태. **하락 추세** 신호 |
| **추세(Trend) / 거시적인 트렌드** | 자잘한 등락을 무시한 '큰 방향'. 이 프로젝트의 핵심 발견: 2023년 상승 → 2024년 3분기부터 하락 전환 |
| **추세적 변동** | 노이즈가 아닌, 방향 자체가 바뀌는 움직임 |
| **하향 이탈 / 추세 이탈** | 종가가 이동평균선 아래로 내려가는 것. 리포트의 실전 규칙: **"종가가 120일 이동평균선을 3% 이상 하향 이탈한 상태가 3영업일 연속 지속되면 보유 비중의 30~50%를 기계적으로 축소(매도)"** |
| **매수 / 매도** | 사는 것 / 파는 것. 이 프로젝트는 매수 타이밍보다 **리스크 관리(매도) 규칙** 에 집중 |
| **장기 포지션 진입** | 단타가 아닌, 길게 들고 가는 투자를 시작하는 것. 이평선 분석의 목적 |

---

## 6️⃣ 수익률과 변동성 (Q2·Q3 검증 도구)

| 용어 | 쉬운 설명 |
|---|---|
| **월별 수익률** | 그 달의 시작 대비 끝 가격이 몇 % 변했는가. 코드: `resample('ME').last().pct_change()`. 12개월치 막대그래프로 그려 계절성(특정 달에 강/약) 확인. 결과: 2024년 7월까지는 양(+)의 수익률 |
| **일일 변동폭(수익률) / 일간 노이즈 / 단기 변동 / 단기 노이즈 / 자잘한 등락** | 하루하루의 오르내림. 대부분 의미 없는 '지터' — 전부 다섯 가지 표현이 같은 개념. 이걸 걸러내야 진짜 추세가 보임 |
| **노이즈** | 신호(추세)를 방해하는 무의미한 잡음. 라디오 잡음처럼 본질이 아닌 것 |
| **롤링 표준편차** | 최근 N일치 표준편차를 매일 새로 계산해 이은 선. "요즘 시장이 얼마나 불안한가"를 시간에 따라 보여줌 |
| **변동성을 계산하는 윈도우 크기** | 표준편차를 잴 때 '며칠치를 볼 것인가'의 구간. 이 프로젝트는 **10일·20일·40일 세 가지**를 비교 |
| **파라미터** | 분석 도구의 '조절 다이얼'. 윈도우 크기(10/20/40), 이탈 기준(3%), 연속 일수(3일) 등 분석자가 정하는 숫자들 |
| **파라미터 민감도 (변동성 분석)** | 다이얼을 바꿀 때 결과가 얼마나 달라지는지 테스트하는 것. 결과: 10일(하늘색)은 너무 민감, 40일(자홍색)은 너무 둔감 → **20일(보라색)이 가장 균형적(Robust)** |
| **다중 윈도우 변동성 지표** | 위처럼 여러 윈도우의 지표를 한 차트에 겹쳐 비교하는 방식 |
| **급등락** | 단기간의 큰 폭 상승·하락 |
| **최대 거래량 / 연간 최대 거래량** | 1년 중 거래량이 가장 많았던 날. 이 프로젝트: **2024년 8월 약 3,000만 주** (평시 약 1,000만 주의 3배) |
| **평시** | 특별한 이벤트가 없는 보통 시기 |
| **주요 지지선 붕괴** | "여기선 안 떨어질 거야"라고 여겨지던 가격대(지지선)가 깨지는 것. 심리적 마지노선 붕괴 |
| **외국인의 대규모 패닉 셀링(투매)** | 공포에 질린 외국인 투자자들이 일제히 던지는 것. 2024년 8~9월 급락의 원인으로 지목 |
| **외인 매도세 / 반도체 섹터 악재 / HBM 기술 경쟁력 / 3분기** | 2024년 3분기 하락의 배경: ① HBM(고대역폭 메모리, AI 반도체 핵심 부품) 기술 경쟁력 논란 ② 반도체 업황 악재 ③ 외국인 매도세 — 이 세 가지가 겹쳐 주가 하락을 만들었다는 정성적 해석 |

---

## 7️⃣ 시계열 분석 (Q4 검증 도구)

| 용어 | 쉬운 설명 |
|---|---|
| **주식 시계열** | 시간 순서대로 쌓인 주가 데이터. "어제와 오늘이 연결된" 데이터라 일반 통계와 다르게 다뤄야 함 |
| **시계열 분해 / 계절성 분해 (Seasonal Decomposition)** | 주가를 3가지 성분으로 쪼개는 기술 (코드: `seasonal_decompose(model='multiplicative', period=20)`). 복잡한 곡선을 "추세+계절성+잔차"로 분해해 각각 따로 봄 |
| **추세(Trend) 성분** | 분해 결과 ①: 장기 방향 (오르는 중? 내리는 중?) |
| **계절성(Seasonal - 20영업일 주기)** | 분해 결과 ②: 약 한 달(20영업일)마다 반복되는 규칙적 물결 |
| **잔차(Resid)** | 분해 결과 ③: 추세와 계절성을 빼고 남은 것 = **순수 노이즈**. 예측 불가능한 부분 |
| **ACF 분석 / 자기상관함수(ACF) 플롯** | "오늘 주가가 N일 전 주가와 얼마나 닮았나"를 1~40일 전까지 계산해 막대로 그린 그래프 (코드: `plot_acf(close_b, lags=40)`) |
| **ACF 플롯 검증** | ACF 막대가 아주 천천히 줄어들면 → 강한 추세 존재. 특정 lag에서만 튀면 → 그 주기의 반복 존재. 이 프로젝트 결과: 완만한 감소 = 추세 중심 시장 |
| **Non-stationary** | '비정상성' — 평균과 분산이 시간에 따라 변하는 상태. 주가의 일반적 특성. ACF가 천천히 감소하는 것이 그 증거 |
| **특정 짧은 주기성** | "며칠마다 반복된다"는 패턴. 이 프로젝트 ACF에서는 뚜렷한 단기 주기 **없음**으로 확인 |
| **랜덤 워크(Random Walk) 성향** | "술 취한 사람의 걸음"처럼 다음 걸음이 예측 불가능한 움직임. 단기 주가가 이 성향을 가진다 = **단기 예측은 불가능**하다는 뜻 |
| **비선형적** | 입력과 출력이 직선(비례) 관계가 아닌 것. 주가는 변수 간 관계가 복잡하게 얽혀 단순 공식으로 설명이 안 됨 |
| **예측 한계** | 위 두 특성 때문에 주가 '정확한 가격' 예측은 불가능. 이 프로젝트가 일부러 단순 예측(Naive)을 시연해 그 한계를 보여준 이유 |

---

## 8️⃣ 예측 관련 (5번 차트)

| 용어 | 쉬운 설명 |
|---|---|
| **Naive 예측(빨간선) / 단순 이동평균 예측** | "미래 = 마지막 20일 평균 그대로"라고 가정하는 가장 단순한 예측. 코드에서 학습 데이터의 마지막 SMA-20 값을 미래 60일에 쭉 펴는 것(빨간 점선) |
| **미래값** | 예측 대상인 아직 안 온 시점의 값 (마지막 60일을 테스트용으로 떼어 둠) |
| **실주가** | 실제로 관측된 주가 (파란 선). 예측(빨간 점선)과 비교해 오차 확인 |
| **평균 오차** | 예측값과 실제값 차이의 평균. 예측 모델의 성적표. Naive 예측의 오차가 크다 = 단순 예측의 한계 증명 |

---

## 9️⃣ 집계 단위와 이중 타임프레임 (6번 차트, 분석 반례 극복)

| 용어 | 쉬운 설명 |
|---|---|
| **집계단위** | 데이터를 묶는 시간 단위 (일간/주간/월간). 같은 데이터도 단위에 따라 완전히 다르게 보임 |
| **일간 데이터 / 일간 주가(회색 선)** | 매일의 종가. 노이즈가 많아 오히려 판단을 흐림 |
| **주간 집계 데이터 / 주간 종가(Weekly Close)** | 일요일~금요일을 하나로 묶어 '그 주의 마지막 종가'만 취한 데이터 (코드: `resample('W').last()`). 노이즈가 걸러져 추세가 선명해짐 |
| **분석 반례 / 주간 집계 분석 반례 / 반례** | "주간으로 묶으면 추세 붕괴를 늦게 알아차리는 거 아냐?" 같은 비판적 질문. 리포트는 일간 차트에선 2024년 8월 급락이 노이즈처럼 보이지만 주간에선 뚜렷하다는 것을 보여주며 반례에 답함 |
| **추세 붕괴 시점** | 상승→하락으로 방향이 꺾이는 순간. 집계 단위에 따라 포착 시점이 달라짐 |
| **주간/일간 차트 비교** | 두 단위를 겹쳐 그려(회색 일간 + 파란 주간) 서로의 장단점을 확인하는 6번 차트 |
| **추세 판단 알고리즘** | "지금이 상승/하락 추세인가?"를 규칙으로 판별하는 절차. 이 프로젝트: 주간 종가로 추세를 보고, 일간 데이터로 진입/청산 타이밍을 잡음 |
| **일간/장중 가격 변동(ATR) 밴드** | 하루 변동 폭(ATR)을 기준으로 그은 위아래 띠. 가격이 밴드 안이면 노이즈, 밖이면 신호로 해석 |
| **이중 타임프레임(Dual Timeframe) 전략** | **주간 = 방향 결정(나침반), 일간 = 타이밍 결정(초시계)**. 두 시간 단위를 동시에 보는 실전 전략. 이 프로젝트의 최종 Action ③ |
| **추종** | 추세 추종: "예측하지 말고, 확인된 추세를 따라간다"는 투자 철학 |

---

## 🔟 한계와 향후 과제 (7장)

| 용어 | 쉬운 설명 |
|---|---|
| **가용성** | 데이터를 실제로 얻을 수 있는지의 여부. 리포트는 주가 외 데이터의 부재를 한계로 인정 |
| **환율** | 원/달러 환율. 외국인 투자자의 매수·매도에 직접 영향 → 향후 ECOS API로 추가 예정 |
| **필라델피아 반도체 지수** | 미국 반도체 섹터 지수(`^SOX`). 삼성전자와 업황을 공유하는 비교 지표 → Yahoo Finance로 추가 예정 |
| **외국인/기관 순매수** | 외국인·기관이 (산 물량 − 판 물량). 투매의 직접 증거 데이터 → KRX에서 추가 예정 |

---

## ✅ 핵심 요약

> **"이 프로젝트는 ① 데이터를 수집·정제(결측치 ffill, 이상치는 Z-score 3/IQR 1.5로 검사)하고, ② 이평선 정배열/역배열로 추세를, ③ 10·20·40일 롤링 표준편차 민감도 비교로 20일 윈도우의 적합성을, ④ 시계열 분해와 ACF로 '추세는 있지만 단기 예측은 불가(랜덤 워크)'를 검증했습니다. ⑤ 결론은 세 가지 정량 규칙입니다 — '120일선 3% 하향 이탈 3일 연속 시 비중 30~50% 축소', '20일 변동성이 6개월 평균의 2.5배 + 거래량 1.5배 급등 시 신규 진입 중단', '주간 종가로 방향, 일간으로 타이밍을 잡는 이중 타임프레임 전략'입니다."**

---

